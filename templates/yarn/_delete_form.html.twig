<form method="post" action="{{ path('app_yarn_delete', {'id': yarn.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ yarn.id) }}">
    <button class="btn">Delete</button>
</form>
