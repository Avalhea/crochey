{% extends 'base.html.twig' %}

{% block title %}ProjectImage{% endblock %}

{% block body %}
    <h1>ProjectImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project_image.id }}</td>
            </tr>
            <tr>
                <th>ImageUrl</th>
                <td>{{ project_image.imageUrl }}</td>
            </tr>
            <tr>
                <th>Caption</th>
                <td>{{ project_image.caption }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_project_image_index') }}">back to list</a>

    <a href="{{ path('app_project_image_edit', {'id': project_image.id}) }}">edit</a>

    {{ include('project_image/_delete_form.html.twig') }}
{% endblock %}
