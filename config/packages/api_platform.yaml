api_platform:
    defaults:
        formats:
            jsonld: [ 'application/ld+json' ]
            json: [ 'application/json' ]
        pagination_items_per_page: 30
        datetime_format: 'Y-m-d'
    graphql:
        enabled: false
    swagger:
        versions: [3]
        api_keys: []
